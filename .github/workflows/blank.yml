name: Build Ubuntu 26.04 arm64 Minimal RootFS with Docker

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # 支持手动触发

jobs:
  build-rootfs:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt update
          sudo apt install -y debootstrap qemu-user-static binfmt-support tar gzip xz-utils rsync coreutils

      - name: Register binfmt for aarch64 emulation
        run: |
          sudo update-binfmts --enable qemu-aarch64
          qemu-aarch64-static --version

      - name: Build Ubuntu 26.04 arm64 rootfs
        env:
          CODENAME: noble
          ARCH: arm64
          ROOTFS_DIR: ./ubuntu2604-arm64-rootfs
          MIRROR: https://mirrors.aliyun.com/ubuntu-ports
        run: |
          set -e
          rm -rf $ROOTFS_DIR
          mkdir -p $ROOTFS_DIR

          # 1. Bootstrap minimal base
          sudo debootstrap \
            --variant=minbase \
            --arch=$ARCH \
            $CODENAME \
            $ROOTFS_DIR \
            $MIRROR

          # 2. Copy qemu-aarch64-static into rootfs for chroot execution
          sudo cp $(which qemu-aarch64-static) $ROOTFS_DIR/usr/bin/

          # 3. Replace apt sources.list 阿里云arm源
          sudo tee $ROOTFS_DIR/etc/apt/sources.list <<EOF
deb ${MIRROR} ${CODENAME} main restricted universe multiverse
deb ${MIRROR} ${CODENAME}-updates main restricted universe multiverse
deb ${MIRROR} ${CODENAME}-backports main restricted universe multiverse
deb http://ports.ubuntu.com/ ${CODENAME}-security main restricted universe multiverse
EOF

          # 4. Basic system config
          echo "ubuntu2604-arm64-docker" | sudo tee $ROOTFS_DIR/etc/hostname
          sudo tee $ROOTFS_DIR/etc/hosts <<EOF
127.0.0.1   localhost ubuntu2604-arm64-docker
::1         ip6-localhost ip6-loopback
EOF

          # DNS
          echo "nameserver 223.5.5.5" | sudo tee $ROOTFS_DIR/etc/resolv.conf
          echo "nameserver 114.114.114.114" | sudo tee -a $ROOTFS_DIR/etc/resolv.conf

          # 5. Chroot 内部执行安装：基础工具 + Docker
          sudo chroot $ROOTFS_DIR /usr/bin/qemu-aarch64-static /bin/bash <<INNER
          set -e
          apt update

          # 基础常用软件包
          apt install -y --no-install-recommends \
            apt-transport-https ca-certificates gnupg2 openssl sudo \
            curl wget git vim-tiny nano tree htop ncdu \
            iproute2 net-tools iputils-ping dnsutils traceroute \
            zip unzip tar xz-utils bzip2 build-essential make gcc g++ cmake tzdata locales

          # 时区上海
          ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
          echo "Asia/Shanghai" > /etc/timezone
          locale-gen zh_CN.UTF-8
          update-locale LANG=zh_CN.UTF-8

          # ========== 安装官方 Docker CE ==========
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
          echo "deb [arch=arm64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu noble stable" | tee /etc/apt/sources.list.d/docker.list
          apt update
          apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

          # 允许sudo执行docker（可选）
          usermod -aG docker root

          # 清理缓存减小体积
          apt autoremove -y
          apt autoclean
          apt clean
          rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
INNER

          # 删除qemu静态二进制（成品rootfs不需要）
          sudo rm -f $ROOTFS_DIR/usr/bin/qemu-aarch64-static

          # 6. 打包成tar.gz
          tar -zcpf ubuntu2604-arm64-full-docker-rootfs.tar.gz -C $ROOTFS_DIR .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ubuntu2604-arm64-rootfs-with-docker
          path: ubuntu2604-arm64-full-docker-rootfs.tar.gz
          retention-days: 30

