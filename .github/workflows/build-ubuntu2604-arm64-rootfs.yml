name: Build Ubuntu 26.04 amd64 RootFS (Locked GNU coreutils Official Source)
run-name: Build resolute amd64 rootfs fixed GNU coreutils

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install build host dependencies
        run: |
          sudo apt update -y
          sudo apt install -y debootstrap qemu-user-static binfmt-support xz-utils

      - name: Prepare workspace dir
        run: mkdir -p rootfs
        working-directory: ${{ github.workspace }}

      - name: Bootstrap Ubuntu 26.04 amd64 official archive
        run: |
          sudo debootstrap \
            --arch=amd64 \
            --include=ca-certificates,openssh-server,locales,tzdata,sudo,vim,curl,wget,git,iproute2,net-tools \
            resolute ./rootfs \
            http://azure.archive.ubuntu.com/ubuntu
        working-directory: ${{ github.workspace }}

      - name: Inject official Ubuntu sources.list
        run: |
          sudo tee ./rootfs/etc/apt/sources.list <<'EOF'
          deb http://azure.archive.ubuntu.com/ubuntu resolute main restricted universe multiverse
          deb http://azure.archive.ubuntu.com/ubuntu resolute-security main restricted universe multiverse
          deb http://azure.archive.ubuntu.com/ubuntu resolute-updates main restricted universe multiverse
          deb http://azure.archive.ubuntu.com/ubuntu resolute-backports main restricted universe multiverse
          EOF
        working-directory: ${{ github.workspace }}

      - name: Bind mount virtual filesystems for chroot
        run: |
          sudo mount --bind /dev ./rootfs/dev
          sudo mount --bind /dev/pts ./rootfs/dev/pts
          sudo mount -t proc none ./rootfs/proc
          sudo mount -t sysfs none ./rootfs/sys
        working-directory: ${{ github.workspace }}

      - name: Chroot install GNU coreutils resolve conflict correctly
        run: |
          sudo chroot ./rootfs /bin/bash -e <<'SCRIPT'
          apt update

          # 写入APT黑名单，后续禁止安装uutils版本
          cat > /etc/apt/preferences.d/block_uutils_coreutils <<APTPIN
          Package: coreutils-from-uutils
          Pin: release *
          Pin-Priority: -10
          APTPIN

          # 第一步：临时解除锁定，允许APT卸载uutils分包
          apt-mark unhold coreutils-from-uutils || true

          # 安装GNU版本，APT自动卸载原uutils虚拟包，解决互斥冲突
          apt install -y coreutils-from-gnu --allow-remove-essential

          # 安装完毕重新上锁，杜绝后续被安装
          apt-mark hold coreutils-from-uutils

          # 固定系统默认调用GNU coreutils
          update-alternatives --set coreutils /usr/bin/coreutils.gnu
          ln -sf /usr/bin/coreutils.gnu /usr/bin/coreutils

          # 关闭APT推荐依赖，杜绝间接被装上uutils
          echo 'APT::AutoRemove::Recommends "false";' > /etc/apt/apt.conf.d/99-block-uutils
          echo 'APT::Install-Recommends "false";' >> /etc/apt/apt.conf.d/99-block-uutils

          # 中文本地化
          locale-gen zh_CN.UTF-8
          echo "LANG=zh_CN.UTF-8" > /etc/default/locale
          echo "LC_ALL=zh_CN.UTF-8" >> /etc/default/locale
          update-locale LANG=zh_CN.UTF-8 LC_ALL=zh_CN.UTF-8

          # SSH允许root密码登录
          sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          systemctl enable ssh

          # root密码 123456
          echo "root:123456" | chpasswd

          # 清理冗余缩小镜像体积
          apt autoremove -y
          apt clean
          rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*
          SCRIPT
        working-directory: ${{ github.workspace }}

      - name: Safe unmount all bind mounts
        run: |
          sudo umount ./rootfs/sys
          sudo umount ./rootfs/proc
          sudo umount ./rootfs/dev/pts
          sudo umount ./rootfs/dev
        working-directory: ${{ github.workspace }}

      - name: Pack rootfs into tar.xz
        run: |
          sudo tar -cJf ubuntu2604-amd64_gnu-coreutils_locked.tar.xz -C ./rootfs .
        working-directory: ${{ github.workspace }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ubuntu2604-amd64-official-rootfs
          path: ubuntu2604-amd64_gnu-coreutils_locked.tar.xz
          retention-days: 30

